move4mobile-experiments

## Setup Environment

### Install Webpack
`npm install -g webpack`



## Deploy Firebase Functions

1. Run `webpack` in ./functions folder
2. Run `firebase deploy --only function` from root